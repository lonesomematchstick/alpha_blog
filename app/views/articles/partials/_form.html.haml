= form_with(model: @article, local: true) do |f|
  .form-fields
    .label
      = f.label :title
    .field
      = f.text_field :title

  %br

  .form-fields
    .label
      = f.label :description
    .field
      = f.text_area :description, size: "30x8"

  %br
  .submit
    - action = @article.persisted? ? "Edit" : "Create"
    = f.submit "#{action} Article"